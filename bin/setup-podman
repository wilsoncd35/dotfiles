#!/usr/bin/env sh

#
# Setup podman.
#

# New virtual machine.
  podman machine init  \
    --cpus 4  \
    --disk-size 100  \
    --memory 8256  \
    --now  \
    --timezone local  \
    podmanvm01
